<!-- 
 ng server --open 启动应用
 declarations:声明本模块中拥有的视图类，ng中有3种：组件，指令，管道
 exports-declaration的子集，可用于其他模块的组件模块
 providers：服务的创建者，并加入到全局服务列表，可用于应用任何部分
 bootstrap：应用的主视图或根组件，它是所有其他视图的宿主，只有根才可以设置bootstrap
 -->